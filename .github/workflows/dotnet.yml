# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: [ "dev" ]
  pull_request:
    branches: [ "dev" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 9.0.x
    - name: Restore dependencies
      run: dotnet restore Training-diary/Training-diary/Training-diary.csproj
    - name: Build
      run: dotnet build Training-diary/Training-diary/Training-diary.csproj --no-restore
    - name: Test
      run: dotnet test Training-diary/TrainingDiaryTest/TrainingDiaryTest.csproj --no-build --verbosity normal
